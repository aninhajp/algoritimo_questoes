#include<stdio.h>
int main(){
	int a, b, h;
	printf("Digite o valor da base e altura do seu retangulo: %d\n", b, h);
	scanf("%d %d", &b, &h);
	a= b*h;
	printf("A area do seu retangulo e: %d\n", a);
	
return 0;	
}
